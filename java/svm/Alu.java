package svm;

public class Alu {

    // 加算
    public byte add(byte a, byte b) {
        return (byte)(a + b);
    }

    // 乗算
    public byte multiply(byte a, byte b) {
        return (byte)(a * b);
    }

}